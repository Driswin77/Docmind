# Node modules
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment variables (contains secrets!)
.env
.env.local
.env.*.local

# Build outputs
dist/
build/
*.log

# OS files
.DS_Store
Thumbs.db

# IDE files
.vscode/
.idea/

# Temporary uploads (multer)
backend/uploads/

# Ollama models (huge files, keep local only)
# (optional, but good to exclude if you accidentally have them)
*.bin
*.gguf